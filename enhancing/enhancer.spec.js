const { succeed, fail, repair, get } = require('./enhancer.js');

// test suite

describe('enhancer.js', () => {
    describe('succeed()', () => {});

    describe('fail()', () => {});

    describe('repair()', () => {});

    describe('get()', () => {});
});
